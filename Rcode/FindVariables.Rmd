---
title: "FindVariables"
output: html_document
date: "2026-09-03"
---

```{r}
library(readr)
library(dplyr)
```

```{r}
files <- list.files(
  "D:/usyd/26s2/STAT3888/ZIPallFiles", # Modify the location
  pattern = "^AHS.*\\.csv$", # # Regex : modify this to filter different files
  full.names = TRUE
)

files
```

```{r}
variable_catalogue <- data.frame()

for (file in unique(files)) {
  
  vars <- names(
    read_csv(
      file,
      n_max = 0, # Read column names
      show_col_types = FALSE
    )
  )
  
  temp <- data.frame( # temporary data frame for one file
    File = basename(file),
    Variable = vars
  )
  
  variable_catalogue <- bind_rows(
    variable_catalogue,
    temp
  )
}
```


```{r}
# Find the location of the variable
variable_catalogue %>%
  filter(Variable == "EGFRKDSA") # Or use regex: grepl("EXREG", Variable)
```

